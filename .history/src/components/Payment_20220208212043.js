import React from "react";

function Payment({ name, price, count, itemSize }) {
let order = [[] {itemSize}, {count}]

console.log(order);
  return (
    <div>
      <p> {order} </p>
    </div>
  );
}

export default Payment;
