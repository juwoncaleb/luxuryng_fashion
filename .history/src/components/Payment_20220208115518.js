import React from 'react';

function Payment({name , count, itemSize}) {
  return <div>
id={items.id}
              key={id}
              name={item.name}
              price={item.price}
              size={item.size}
              image1={item.image1}
              itemSize={item.itemSize}
              count={item.count}
  </div>;
}

export default Payment;
